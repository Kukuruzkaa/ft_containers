#include <iostream>
#include <iterator>

//typedef struct {
//              int qi;
//              int taille;
//              std::string name;
//      } person;

int main() {
        person florian;

        florian.qi = 250000;
        florian.taille = 179;
        florian.name = "super bo gosse";

        person daria;
        daria.qi = 90;
        daria.taille = 150;
        daria.name = "super belle gosse";

        std::cout << "qui es-tu ? " << florian.name << std::endl;
        std::cout << "et toi ? " << daria.name << std::endl;
}

//struct iterator_traits<char>

//struct iterator_traits<person>
//
/
class Person {
        std::string name;
        typedef struct input_iterator_tag iterator_category;
        typedef int value_type;
        ...

}

/*struct iterator_traits<Person> {
---> typedef input_iterator_tag iterator_category;
        typedef int value_type;
...
}*/

struct iterator_traits<daria*> {
        typedef random_access_iterator_tag iterator_category;
        typedef daria value_type;
        typedef ptrdiff_t difference_type;
        typedef daria* pointer;
        typedef daria& reference;
}

daria* x;
daria* y;
